require 'spec_helper'

describe Wiot::Sdk do
  it 'has a version number' do
    expect(Wiot::Sdk::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
